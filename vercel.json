{
  "version": 2,
  "builds": [
    { "src": "proxy_handler.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/proxy_handler.py" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
